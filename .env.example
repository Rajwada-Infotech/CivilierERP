# Database (SQL Server) - EDIT THESE!
DB_USER=sa
DB_PASSWORD=yourpassword
DB_SERVER=localhost,1433
DB_NAME=CivilierERP

# Redis (auto-set by docker-compose)
# REDIS_HOST=redis
# REDIS_PORT=6379

# App
NODE_ENV=production
PORT=5000

# JWT etc (keep your existing)
JWT_SECRET=your_jwt_secret_here

