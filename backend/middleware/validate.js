// validate.js — alias so require("../middleware/validate") resolves correctly.
// The implementation lives in validateRequest.js — do not duplicate logic here.
module.exports = require("./validateRequest");