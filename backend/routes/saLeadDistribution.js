const express=require("express");const router=express.Router();const{getPool}=require("../db");const{requirePageRight}=require("../middleware/requirePageRight");router.get("/",requirePageRight("sa-lead-distribution","view"),async(req,res)=>{try{const pool=await getPool();const r=await pool.request().query(`SELECT d.Id,d.LeadId,d.FromUserId,d.ToUserId,d.Level,d.Method,d.DistributedAt,d.DistributedBy,l.LeadUid,l.CustomerName,l.Mobile,fu.name AS FromUserName,tu.name AS ToUserName FROM dbo.SaLeadDistribution d JOIN dbo.SaLead l ON d.LeadId=l.Id LEFT JOIN dbo.Users fu ON d.FromUserId=fu.id LEFT JOIN dbo.Users tu ON d.ToUserId=tu.id ORDER BY d.DistributedAt DESC`);res.json(r.recordset);}catch(e){res.status(500).json({error:e.message});}});router.get("/pending",requirePageRight("sa-lead-distribution","view"),async(req,res)=>{try{const pool=await getPool();const level=parseInt(req.query.level)||1;const r=await pool.request().input("level",level).query(`SELECT l.Id,l.LeadUid,l.CustomerName,l.Mobile,l.Status,l.DateGenerated,l.PlatformId,l.CampaignId,p.Name AS PlatformName,c.Name AS CampaignName FROM dbo.SaLead l LEFT JOIN dbo.SaSocialMediaPlatform p ON l.PlatformId=p.Id LEFT JOIN dbo.SaCampaign c ON l.CampaignId=c.Id WHERE l.IsActive=1 AND (@level=1 AND l.Status='New' OR @level=2 AND l.Status='Assigned' AND l.AssignedTeamLeadId IS NOT NULL) ORDER BY l.DateGenerated ASC`);res.json(r.recordset);}catch(e){res.status(500).json({error:e.message});}});router.post("/distribute",requirePageRight("sa-lead-distribution","create"),async(req,res)=>{try{const pool=await getPool();const{leadIds,assignments,method,level,distributedBy}=req.body;if(!Array.isArray(leadIds)||!Array.isArray(assignments)||!leadIds.length||!assignments.length)return res.status(400).json({error:"leadIds and assignments required"});const tx=pool.transaction();await tx.begin();try{for(const asgn of assignments){const{userId,leadIdList}=asgn;for(const lid of leadIdList){await tx.request().input("lid",lid).input("fuid",distributedBy||null).input("tuid",userId).input("lvl",level||1).input("mth",method||"Equal").input("dby",distributedBy||null).query("INSERT INTO dbo.SaLeadDistribution(LeadId,FromUserId,ToUserId,Level,Method,DistributedAt,DistributedBy)VALUES(@lid,@fuid,@tuid,@lvl,@mth,SYSDATETIME(),@dby)");if(level===1){await tx.request().input("lid",lid).input("tuid",userId).query("UPDATE dbo.SaLead SET AssignedTeamLeadId=@tuid,Status='Assigned',UpdatedAt=SYSDATETIME()WHERE Id=@lid");}else{await tx.request().input("lid",lid).input("tuid",userId).query("UPDATE dbo.SaLead SET AssignedSalespersonId=@tuid,Status='Assigned',UpdatedAt=SYSDATETIME()WHERE Id=@lid");}}}await tx.commit();res.json({success:true,distributed:leadIds.length});}catch(e){await tx.rollback();throw e;}}catch(e){res.status(500).json({error:e.message});}});module.exports=router;